<UserControl x:Class="desktop_app.Views.FormBookingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:desktop_app.Views"
             xmlns:viewModel="clr-namespace:desktop_app.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" 
             d:DesignWidth="800"
             Style="{StaticResource UserControlBase}"
             DataContext="{x:Static viewModel:FormBookingViewModel.Instance}">
    
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Style="{StaticResource HeaderWrapper}">
            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource HeaderMainTitle}"/>
            
                <Border Style="{StaticResource IdWrapper}">
                    <TextBlock Style="{StaticResource UpdateIdStyle}" Text="{Binding Booking.Id}"/>
                </Border>
            </StackPanel>
        </Border>
        
        <StackPanel Grid.Row="1" Style="{StaticResource BtnWrapper}">
            <Button Content="Volver" Style="{StaticResource RedReturnBtn}" Command="{Binding ReturnCommand}"/>
            <Button Content="Guardar" Style="{StaticResource SaveBtn}" Command="{Binding SaveCommand}"/>
            <Button Content="Cancelar" Style="{StaticResource CancelBtn}" Command="{Binding CancelCommand}" IsEnabled="{Binding Disabled}"/>
        </StackPanel>
        
        <Grid Grid.Row="2" Margin="0 15 0 0" Width="500" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel>
                <Label Content="DNI del cliente: " Target="DniTextBox" Style="{StaticResource LabelForm}"/>
                <TextBox x:Name="DniTextBox" Text="{Binding ClientDni, Mode=TwoWay}" Style="{StaticResource TextBoxForm}" IsEnabled="{Binding Enabled}"/>
            </StackPanel>
            
            <StackPanel Grid.Row="1">
                <Label Content="Número de la habitación: " Target="RoomNumberTextBox" Style="{StaticResource LabelForm}"/>
                <TextBox x:Name="RoomNumberTextBox" Text="{Binding Booking.RoomNumber, Mode=TwoWay}" Style="{StaticResource TextBoxForm}" IsEnabled="{Binding Enabled}"/>
            </StackPanel>
            
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Stretch">
                <Grid Width="500">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel>
                        <Label Content="Fecha de inicio: " Target="FirstNightDatePicker" Style="{StaticResource LabelForm}"/>
                        <DatePicker x:Name="FirstNightDatePicker" SelectedDate="{Binding Booking.CheckInDate, Mode=TwoWay}" Language="es-Es" Style="{StaticResource DatePickerForm}" Margin="0 0 10 0"/>
                    </StackPanel>
            
                    <StackPanel Grid.Column="1">
                        <Label Content="Fecha de fin: " Target="LastNightDatePicker" Style="{StaticResource LabelForm}"/>
                        <DatePicker x:Name="LastNightDatePicker" SelectedDate="{Binding Booking.CheckOutDate, Mode=TwoWay}" Language="es-Es" Style="{StaticResource DatePickerForm}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
            
            <StackPanel Grid.Row="3">
                <Label Content="Cantidad de huéspedes: " Target="GuestsTextBox" Style="{StaticResource LabelForm}"/>
                <TextBox x:Name="GuestsTextBox" Text="{Binding Booking.Guests, Mode=TwoWay}" Style="{StaticResource TextBoxForm}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
